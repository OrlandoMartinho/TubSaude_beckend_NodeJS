import Emails from './Emails.js';

export default class Instituto extends Emails {
    
    constructor(senha,nome) {
        
        super(id,email); 
        this.senha = senha;
        this.nome = nome;
        

    }

}